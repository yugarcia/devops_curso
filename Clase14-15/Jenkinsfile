pipeline {
    agent any
    stages {
        stage('SCM checkout') {
            steps {
                git branch: 'master', url: ''
            }
        }

        stage('Execute ansible') {
            steps {
                ansiblePlaybook colorized: true, installation: 'ansible', inventory: 'hosts', playbook: 'playbook.yml'
            }
        }
    }
}